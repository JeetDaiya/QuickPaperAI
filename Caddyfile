80-225-247-83.sslip.io {
    reverse_proxy app:8000

    encode gzip

    log {
        output stdout
        format json
    }
}

# Additional sites (not managed by this repo) live in ./sites/*.Caddyfile on the VM.
import sites/*.Caddyfile
