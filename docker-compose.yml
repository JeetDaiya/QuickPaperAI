services:
  app:
    image: ghcr.io/jeetdaiya/quickpaperai:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./firebase-credentials.json:/app/firebase-credentials.json:ro
      - paper_outputs:/app/outputs
    expose:
      - "8000"
    networks:
      - webnet

  worker:
    image: ghcr.io/jeetdaiya/quickpaperai:latest
    restart: unless-stopped
    command: arq src.paper.worker.settings.WorkerSettings
    env_file:
      - .env
    volumes:
      - ./firebase-credentials.json:/app/firebase-credentials.json:ro
      - paper_outputs:/app/outputs
    networks:
      - webnet

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./sites:/etc/caddy/sites
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - webnet

networks:
  webnet:

volumes:
  caddy_data:
  caddy_config:
  paper_outputs:
